# src-tauri 打包日志

- 更新时间：2026-06-05 23:02:04
- 版本号：0.1.1

## 本次执行

- 执行 `npm run tauri build`
- 成功生成：
  - `target/release/bundle/nsis/ai-experts_0.1.1_x64-setup.exe`
  - `target/release/bundle/msi/ai-experts_0.1.1_x64_en-US.msi`

## 打包备注

- Rust `release` 构建完成，存在若干未使用函数/字段警告，但不影响产物生成。
- Tauri 提示 `identifier` 以 `.app` 结尾不推荐。

## 逆向说明

1. 删除本次生成的 `bundle/nsis` 与 `bundle/msi` 对应安装包即可回退产物层结果。
2. 若需要代码层回退，请参考根目录同名打包日志和页签修复日志。

## 验证

- 打包完成，产物路径有效。
