# src-tauri 版本同步日志

- 更新时间：2026-06-05 18:29:52
- 版本号：0.1.1

## 变更内容

- 修改 `tauri.conf.json` 版本号为 `0.1.1`。
- 修改 `Cargo.toml` 包版本为 `0.1.1`。
- 修改 `Cargo.lock` 中当前应用包 `ai-experts` 的版本记录为 `0.1.1`。
- 修改 `src/lib.rs` 中默认 `.xt/config.json` 初始化模板的版本号为 `0.1.1`。

## 逆向说明

- 若需回退，只需将以上文件中的 `0.1.1` 恢复为 `0.1.0`，并保留其他依赖版本不变。

## 验证

- 前端构建验证通过，本次未单独执行 Rust 打包。
