# src 仓库与 Git 入口恢复日志

- 更新时间：2026-06-05 23:33:19
- 版本号：0.1.1

## 文件变更

### `main.ts`

- 新增 `show-error` 事件监听，修复侧栏和其他模块派发错误事件后前端不显示提示的问题。
- 新增 `resolveActiveProjectForView()`，为 `仓库` / `Git` 入口补充项目恢复与兜底打开逻辑。
- 将 `enterWikiMode()`、`enterGitMode()` 接入新的项目恢复函数。

### `sidebar.ts`

- 新增 `getFirstChat()`，用于在活跃项目丢失时恢复第一个可用项目。

## 逆向说明

1. 删除 `resolveActiveProjectForView()` 与 `show-error` 监听。
2. 恢复 `enterWikiMode()`、`enterGitMode()` 的旧前置判断。
3. 删除 `getFirstChat()`。

## 验证

- `npm run build` 通过。
