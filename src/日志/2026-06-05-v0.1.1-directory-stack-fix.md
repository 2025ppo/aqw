# src 可视化目录层级修复日志

- 更新时间：2026-06-05 23:47:12
- 版本号：0.1.1

## 文件变更

### `main.ts`

- 将目录相关显示/隐藏锚点从 `canvas-directory-card` 改为 `canvas-directory-stack`。
- 同步修正草稿、图像、视频、数据等模式切换时目录浮层的显示控制。

### `styles.css`

- 新增 `canvas-directory-stack` 样式与高层级点击区。
- 将目录卡头部改成纵向排布，避免 `可视化目录` 与 `目录 / 草稿` 重叠。
- 将控制条改为目录卡下方布局，并在草稿模式下保留可见与可点击。

### `index.html`

- 重排目录卡与控制条 DOM 结构，形成新的目录浮层栈。

## 逆向说明

1. 删除 `canvas-directory-stack` 相关结构和样式。
2. 恢复目录卡、控制条在旧位置的 DOM 与 CSS。
3. 恢复 `main.ts` 中旧的目录卡显示/隐藏控制。

## 验证

- `npm run build` 通过。
