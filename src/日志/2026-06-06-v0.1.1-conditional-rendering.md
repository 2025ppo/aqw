# src 主视图条件渲染修复日志

- 更新时间：2026-06-06 11:17:25
- 版本号：0.1.1

## 文件变更

### `main.ts`

- 新增 `setFloatingActionsVisible()`。
- 将目录工作区显示/隐藏改为真实 `display` 控制。
- 将图像、视频、数据、项目设置等主视图改为“只显示当前页”的切换逻辑。
- 清理旧的 `style.visibility` 路径，减少页面叠层残留。

### `styles.css`

- 将软件设置页和项目设置页改为实底背景。
- 将设置侧栏与内容区改为不透底面板。

## 逆向说明

1. 恢复 `visibility` 切换逻辑。
2. 恢复设置页旧的透明背景。
3. 恢复各主视图原先的遮挡式切换行为。

## 验证

- `npm run build` 通过。
