# src 设置入口拆分日志

- 更新时间：2026-06-06 11:02:00
- 版本号：0.1.1

## 文件变更

### `main.ts`

- 新增 `workspace-settings` 主视图类型。
- 新增 `workspaceSettingsPanel` / `workspaceSettingsBackBtn` 控制逻辑。
- 新增 `activateWorkspaceSettingsSection()` 与 `enterWorkspaceSettingsMode()`。
- 软件设置打开时只同步主题，不再顺带加载项目配置数据。

### `styles.css`

- 新增 `workspace-settings-panel` 工作区样式。
- 新增项目设置摘要卡样式。
- 保持原有设置组件风格，但拆分到两个独立入口。

### `index.html`

- 新增顶部 `设置` 页签按钮。
- 将 `settings-page` 缩减为软件级设置。
- 新增 `workspace-settings-panel` 承载项目级配置。

## 逆向说明

1. 删除 `workspace-settings-panel` 与顶部设置页签。
2. 将专家团/密钥池配置移回原 `settings-page`。
3. 删除项目设置视图相关逻辑。

## 验证

- `npm run build` 通过。
