# 版本 0.1.1 项目设置对比度修复日志

- 时间：2026-06-06 11:44:00
- 版本号：0.1.1
- 模块：src

## 修改内容

1. 调整 `expert-studio-hero` 的背景与边框层级，避免标题区出现深底深字或浅底浅字。
2. 调整 `expert-bulk-card`、`expert-stat-card` 的表面、描边和阴影，让统计卡与配置卡颜色统一。
3. 调整 `expert-studio-title`、`expert-studio-desc`、`expert-panel-meta`、`expert-stat-label` 等文字颜色，提升正文和辅助信息对比度。
4. 调整 `expert-search-box`、`model-config-select`、`expert-filter-chip`、`expert-ghost-btn` 等控件的背景与文字色，修复输入区和筛选区发灰发虚问题。
5. 保持改动仅限 `styles.css`，不引入新的主程序结构变更。

## 涉及文件

- `styles.css`

## 回退说明

1. 回退时优先恢复 `styles.css` 中新增的主题覆盖段落。
2. 若需要更稳妥回退，可整体恢复 `styles.css` 到本日志生成前的版本。

## 验证记录

1. `npm run build` 通过。
2. 本地预览中已检查“项目设置 / 专家团配置台”页面对比度效果。
