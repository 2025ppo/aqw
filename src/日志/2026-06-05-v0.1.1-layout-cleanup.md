# src 布局清理日志

- 更新时间：2026-06-05 19:40:40
- 版本号：0.1.1

## 文件变更

### `styles.css`

- 重设工作区变量：
  - `--workspace-top`
  - `--workspace-card-radius`
- 调整顶部视图栏、主画布、目录卡片、文件卡片、草稿工具栏、草稿画布、右侧栏的锚点和尺寸关系。
- 新增 `2026-06 harmony pass`，统一主要卡片和弹层的背景、边框、阴影与文字颜色。
- 在草稿模式下隐藏 `canvas-legend`，进一步清理画布。

### `main.ts`

- 增加 `workspace-draft-mode` 的 body 状态切换，用于草稿态布局收口。

### `index.html`

- 将设置页官网链接和若干左侧色条切换为主题变量，避免卡片系统色彩分裂。

## 逆向说明

1. 删除 `styles.css` 末尾的 `2026-06 harmony pass` 区块。
2. 回退 `main.ts` 中 `workspace-draft-mode` 的 class 切换。
3. 恢复 `index.html` 原本的内联颜色值。

## 验证

- `npm run build` 通过。
