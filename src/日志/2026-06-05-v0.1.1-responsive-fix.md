# src 响应式与层级修复日志

- 更新时间：2026-06-05 19:57:30
- 版本号：0.1.1

## 文件变更

### `styles.css`

- 引入工作区横向尺寸变量：
  - `--chat-card-width`
  - `--right-rail-width`
  - `--draft-sidebar-width`
  - `--panel-gap`
- 调整主画布、聊天区、右侧面板、草稿工具栏的锚点计算。
- 修复 `canvas-directory-card` / `canvas-file-card` 以容器坐标定位。
- 新增中窄窗口断点规则，提升自适应能力。
- 统一主要卡片的灰白层级关系，增强内外表面区分。

### `main.ts`

- 继续使用 `workspace-draft-mode` 作为草稿态布局收口状态。

## 逆向说明

1. 删除本次新增的尺寸变量与断点规则。
2. 恢复目录/文件卡片旧的 `workspace-left` 定位方式。
3. 如需整体回退，请同时参考前几份 0.1.1 的 UI 日志。

## 验证

- `npm run build` 通过。
