# src 交互与视觉更新日志

- 更新时间：2026-06-05 19:01:28
- 版本号：0.1.1

## 文件变更

### `draft.ts`

- 修复 `resize()`：
  - 由 `draft-canvas` 父级尺寸改为实际 `canvas-container` 尺寸。
  - 使用 `setTransform(1,0,0,1,0,0)` 重置上下文，避免重复缩放叠加。
- 修复 `activate()`：
  - 激活草稿层时立即重算画布布局，减少错位。

### `main.ts`

- `applyTheme()` 改为统一设置 `document.documentElement.dataset.theme`。
- `dir-view-directory` 改为显式恢复正常目录层。
- `dir-view-draft` 改为显式进入草稿层并刷新卡片状态。

### `styles.css`

- 新增轻量现代化设计令牌与暗色主题变量。
- 新增 `2026-06 shell refresh` 覆盖区，统一重塑主界面外壳。
- 重写工作区定位变量，统一 `floating-actions`、`canvas-container`、`draft-canvas`、右侧面板的空间坐标。

### `sidebar.css`

- 新增 `2026-06 sidebar refresh` 覆盖区。
- 统一侧边栏、项目项、操作菜单和输入框的主题表现。

## 逆向说明

1. 回退草稿错位问题：撤销 `draft.ts` 中 `resize()` 与 `activate()` 的本次调整。
2. 回退主题系统：恢复 `main.ts` 旧版 `applyTheme()` 的逐节点样式写法。
3. 回退现代化视觉层：删除 `styles.css` 和 `sidebar.css` 中新增的 `2026-06` 覆盖区。

## 验证

- 已通过 `npm run build`。
