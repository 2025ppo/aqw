# src 前端交互更新日志

- 更新时间：2026-06-05 18:29:52
- 版本号：0.1.1

## 代码改动

- `main.ts`
  - 新增 `directorySurfaceMode`，区分目录层与草稿层。
  - 新增 `updateDirectorySurfaceUI()`，统一维护目录卡片标题、副标题、按钮激活态。
  - 将 `enterDraftMode()` / `exitDraftMode()` 改为维持“目录页”主视图激活，不再依赖顶栏独立草稿按钮。
  - 新增目录卡片内 `dir-view-directory`、`dir-view-draft` 事件绑定。
- `styles.css`
  - 增强目录卡片的层级感和边框表现。
  - 新增目录/草稿切换器样式。
  - 新增草稿态目录卡片高亮样式。
  - 新增目录副标题样式。

## 逆向说明

1. 删除 `directorySurfaceMode` 与 `updateDirectorySurfaceUI()`。
2. 还原 `enterDraftMode()` 中对 `btn-draft` 的依赖。
3. 删除 `.canvas-directory-topbar`、`.canvas-view-switch*`、`.canvas-directory-subtitle` 等新增样式。

## 验证

- `npm run build` 通过。
