- **文档解析**：集成 `lopdf` (PDF), `calamine` (Excel/CSV), `docx-rs` (Word) 实现多格式内容提取。
- **检索算法**：自研 TF-IDF 向量空间模型与基于启发式规则（imports/calls/references）的代码静态分析图谱。
- **AI 集成**：通过 `reqwest` 调用 DeepSeek API，采用“原始信号→Knowledge Card→RepoWiki”的两层凝练 Prompt 工程架构。