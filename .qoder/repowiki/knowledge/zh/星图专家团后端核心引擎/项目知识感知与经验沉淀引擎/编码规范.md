- 采用“信号-卡片-文章”的知识生产范式，先由 AI 从代码片段生成高密度 JSON 知识卡片，再二次凝练为人类可读的 Markdown Wiki。
- 实施严格的索引构建保护机制，包括单文件分段上限（50 chunks）、总分段上限（50,000 chunks）及图谱边数上限（100,000 edges）以防止资源溢出。
- 在代码图谱构建中利用 HashMap 预建索引（如 stem_to_chunks），将原本 O(n²) 的跨文件引用匹配优化为 O(n) 复杂度。
- 统一使用 `.xt` 隐藏目录作为项目级元数据存储根路径，下设 `perceptual_index`、`repo/cards`、`deliverables` 等子目录进行结构化持久化。