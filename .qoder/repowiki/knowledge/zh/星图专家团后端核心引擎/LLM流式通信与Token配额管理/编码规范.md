- 所有对外暴露的数据结构均使用`serde`进行序列化标记，并统一采用`#[serde(rename_all = "camelCase")]`以适配前端JSON规范。
- 网络请求模块统一内置指数退避重试逻辑（`RetryConfig`），针对429/5xx等状态码自动触发重连。
- 业务逻辑层（如配额检查、仪表盘聚合）采用无状态的纯函数设计，输入输出均为POD（Plain Old Data）结构，便于单元测试与跨上下文复用。