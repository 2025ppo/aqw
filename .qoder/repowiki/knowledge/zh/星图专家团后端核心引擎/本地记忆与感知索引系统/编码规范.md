- 所有模块间的数据交换结构体（如 `MemoryEntry`, `CodeChunk`, `SearchResult`）均强制派生 `Serialize` 和 `Deserialize` 以支持 JSON 持久化。
- 在涉及文件系统读写或索引加载的操作中，统一返回 `Result<T, String>` 类型，并将底层错误转换为描述性字符串。
- 在处理大规模代码库时，普遍采用总量保护机制（如 `MAX_ENTRIES_PER_TYPE`, `MAX_TOTAL_CHUNKS`），防止内存溢出或索引爆炸。