- 所有工具实现均遵循 `ToolExecutor` Trait 契约，通过 `definition()` 暴露 JSON Schema 参数定义，并通过 `execute()` 处理异步逻辑。
- 文件路径操作前必须调用 `validate_path` 或等效逻辑，确保目标路径位于 `project_dir` 范围内且不含 `..` 穿越符。
- Shell 命令执行结果统一封装为 `ExecOutput` 结构，包含退出码、耗时、截断状态及格式化后的模型可读文本。
- 补丁解析采用“先验证后执行”策略，并在应用更新时通过计算替换位置并从后向前操作来避免行号偏移导致的错误。