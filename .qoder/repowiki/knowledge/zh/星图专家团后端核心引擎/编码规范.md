- 所有对外暴露的 Tauri 命令函数均遵循 `async fn command_name(...) -> Result<String, String>` 签名，返回 JSON 序列化字符串。
- 核心业务逻辑模块（如 expert_*, pipeline_*）均采用独立的 `.rs` 文件组织，并在 `lib.rs` 中通过 `mod` 声明进行集中注册。
- 跨模块数据结构统一使用 `serde` 的 `#[serde(rename_all = "camelCase")]` 属性，确保与前端 TypeScript 接口的命名风格一致。