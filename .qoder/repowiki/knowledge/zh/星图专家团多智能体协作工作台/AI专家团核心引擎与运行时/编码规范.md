- 采用分层配置加载策略，优先级遵循：内置默认 < 用户全局 (~/.xt) < 项目级 (.xt/project-config.json) < 运行时覆盖。
- 在 `blackboard_engine` 中使用 `[ACTION:...]` 正则标记提取专家意图，并通过进度签名（Progress Signature）机制检测并阻断空转循环。
- 提示词工程采用模块化拼装模式，根据专家身份（如工程/审查/创意）和任务关键词动态注入 `CODE_TOOL_PRIMER` 或 `WEB_SEARCH_GUIDANCE` 等指令片段。