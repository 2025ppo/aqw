- 所有跨模块交互的数据结构均使用 `#[serde(rename_all = "camelCase")]` 确保与前端或 JSON 协议的兼容性。
- 核心业务逻辑函数（如 `build_expert_task_payload`、`evaluate_step_deliverable_guard`）均采用纯函数设计，通过 Request/Response 结构体传参以支持单元测试。
- 在 `workflow_engine` 中统一使用 `[ACTION:OPERATION:path]` 自定义标记语法来标识文件系统的增删改操作。