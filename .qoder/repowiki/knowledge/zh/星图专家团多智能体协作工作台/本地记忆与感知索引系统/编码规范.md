- 所有涉及项目数据的函数均以 `project_dir: &Path` 或 `&PathBuf` 作为首要参数，通过拼接 `.xt` 子目录实现数据隔离。
- 数据结构普遍派生 `Serialize` 和 `Deserialize` trait，并配合 `serde_json` 进行文件的读写与持久化。
- 错误处理统一使用 `Result<T, String>` 模式，通过 `map_err` 将底层 IO 或解析错误转换为描述性的中文字符串。
- 在执行搜索或检索前，通常会先检查索引文件或记忆文件是否存在，若不存在则返回空集合或默认状态，避免程序崩溃。