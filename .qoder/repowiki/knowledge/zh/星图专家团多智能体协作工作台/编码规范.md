- 后端命令函数统一采用 `_runtime` 后缀区分带配额检查的增强版本（如 `supervisor_analyze_dispatch` 与 `supervisor_analyze_dispatch_runtime`）。
- 前后端通信载荷严格遵循 `camelCase` 序列化约定，通过 `serde` 宏在 Rust 结构体中显式声明。
- 专家引擎模块采用“身份-会话-工具-后置处理”的分层设计模式，确保各子引擎职责单一且可独立测试。