- 采用 Request/Response DTO 模式，所有公共函数的输入参数均封装为带有 `#[serde(rename_all = "camelCase")]` 的结构体，以适配前端通信协议。
- 在涉及专家身份展示时，统一使用 `expert_label` 辅助逻辑将 ID 格式化为“名称（头衔）”的可读字符串，确保 UI 呈现的一致性。
- 状态变更遵循不可变更新原则，通过接收当前 `state` 并返回新 `state` 的方式（如 `apply_pipeline_task_outcome`）来管理流水线生命周期，避免隐式副作用。