- 所有 LLM 请求与响应结构均派生 `serde::{Serialize, Deserialize}` 以实现 JSON 序列化。
- 流式调用通过 Tauri `AppHandle::emit` 发送特定事件名（如 `llm-stream-token`）实现前后端解耦。
- 配额检查逻辑统一返回 `QuotaCheckResponse`，包含 `allowed` 布尔值及阻断原因。
- 网络搜索优先尝试 RSS 格式解析，失败后回退至 HTML 解析以提升稳定性。