- 采用单例模式管理全局状态与工具类（如`getConfig()`、`getContextManager()`）。
- 通过`invoke`调用后端Rust指令处理持久化与复杂计算，前端仅负责UI渲染与状态同步。
- 使用`data-theme`属性配合CSS变量实现全局主题切换，避免硬编码样式。
- 在`main.ts`中集中定义跨模块共享的TypeScript接口（如`TokenUsageRecord`、`TreeEntry`）。