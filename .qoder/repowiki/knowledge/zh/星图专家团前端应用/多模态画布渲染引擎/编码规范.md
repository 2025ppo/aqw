- 所有画布类均通过监听 `wheel`、`mousedown/move/up` 事件手动实现视口平移与以鼠标为中心的缩放逻辑。
- 渲染方法（`render`）通常先清空容器（`innerHTML = ""` 或移除子元素），再根据当前数据状态重新创建并追加 SVG 或 HTML 元素。
- 跨模块通信倾向于使用全局事件总线（`window.dispatchEvent` / `addEventListener`）或挂载到 `window` 上的命名空间对象。