- 采用防抖策略处理数据持久化，通过 `debouncedSave` 方法在用户操作结束后延迟写入，避免频繁 I/O 开销。
- 统一使用 `worldToScreen` 和 `screenToWorld` 坐标转换工具函数，确保所有绘图逻辑与 DOM 元素定位均基于世界坐标系计算。
- 在鼠标事件处理中广泛使用 `passive: false` 选项并调用 `preventDefault`，以精确控制画布滚动、缩放及右键平移等自定义交互行为。