- 采用 `*_runtime` 后缀区分带配额检查与 Token 追踪的命令变体（如 `supervisor_analyze_dispatch` 与 `supervisor_analyze_dispatch_runtime`）。
- 所有跨模块数据结构均使用 `serde` 的 `#[serde(rename_all = "camelCase")]` 属性，确保与前端 JSON 交互的命名一致性。
- 工具调用结果统一封装为 `ExpertToolExecutionResult`，并通过 `event` 字段向 UI 推送实时进度与状态变更。
- 专家身份与职责通过 `discipline-XXX` 格式的 ID 进行标准化映射，并在 `expert_identity` 模块中集中管理分类逻辑。