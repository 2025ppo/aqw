- 采用 IIFE（立即调用函数表达式）封装 JS 模块逻辑，避免污染全局命名空间，仅通过特定命名空间（如 `window.StarAtlasExpertsUI`）暴露必要接口。
- 使用 CSS 自定义属性（CSS Variables）统一管理深色/浅色主题的色彩令牌（Tokens），确保全站视觉风格的一致性。
- 在 JavaScript 中通过 `document.createElement` 和模板字符串动态构建复杂的 DOM 结构（如专家卡片），而非在 HTML 中硬编码重复内容。