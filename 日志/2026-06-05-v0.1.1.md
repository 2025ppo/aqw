# ai-experts 模块更新日志

- 更新时间：2026-06-05 18:29:52
- 版本号：0.1.1

## 变更摘要

- 修改 `index.html`：
  - 在可视化目录卡片内新增 `Workspace Atlas` 顶部区。
  - 新增 `目录 / 草稿` 视图切换按钮。
  - 删除顶部悬浮视图栏中的独立 `草稿` 按钮。
  - 设置页版本展示改为 `0.1.1`。
- 修改 `package.json`、`package-lock.json`：
  - 包版本更新为 `0.1.1`。

## 逆向说明

- 如需回退本模块改动，优先恢复 `index.html` 的旧草稿入口，并将版本号改回 `0.1.0`。

## 验证

- 已执行构建验证，结果通过。
