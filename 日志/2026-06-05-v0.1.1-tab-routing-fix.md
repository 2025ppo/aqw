# ai-experts 页签路由修复日志

- 更新时间：2026-06-05 22:50:58
- 版本号：0.1.1

## 变更摘要

- 调整 `src/main.ts`：
  - 把顶部主视图页签的事件绑定前移。
  - 新增统一主视图切换函数，收口 `目录 / 文件 / 仓库 / Git / 词元` 的进入逻辑。
  - 提前暴露 `window.__switchView`，减少后续初始化异常对页签切换的影响。
  - 为窗口控制和拖拽监听增加安全降级。
- 调整 `src/styles.css`：
  - 修复可视化目录卡片头部布局拥挤与遮挡。
- 保持 `index.html` 顶部页签 `data-view` 作为唯一视图映射来源。

## 逆向说明

- 回退时优先参考 `src/日志/2026-06-05-v0.1.1-tab-routing-fix.md` 的文件级说明。

## 验证

- `npm run build` 通过。
